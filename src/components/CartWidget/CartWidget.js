const CartWidget = () => {
      return (
          <button>
               <img src={'./images/cart.svg'} alt='cart-widget'/>  
               0
          </button>
    )
}

export default CartWidget